# AV Food Factory — Homepage (Next.js + Tailwind + TS)

Single, conversion-first homepage with Lucknowi/Nawabi vibe.

## Quick start
```bash
npm install
npm run dev
# open http://localhost:3000
```

## Notes
- Next.js App Router, TypeScript, Tailwind 3.
- Color tokens in `tailwind.config.ts` (`nawab.*`).
- Edit sections in `/components` and content in `Hero`, `MenuPreview`, etc.
# av-food-factory
